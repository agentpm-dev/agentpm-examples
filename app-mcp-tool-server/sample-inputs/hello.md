# Hello MCP

This is a sample local document for the generated MCP server template.

Use it to test:

- `document-convert`
- `summarize-text`
- `translate-text`

The goal is to provide one low-friction local file that proves the server can expose useful tools before you wire it into a real client.
